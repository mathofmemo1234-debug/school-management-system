rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Comprehensive open access for School System operations across all collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
